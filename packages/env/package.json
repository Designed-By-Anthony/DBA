{
  "name": "@dba/env",
  "version": "0.1.0",
  "private": true,
  "description": "Zod-validated environment schemas for every Designed by Anthony surface. Forces the build to fail when a subdomain is missing a required key instead of shipping a White Screen of Death.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./web-viewer": "./src/web-viewer.ts",
    "./marketing": "./src/marketing.ts",
    "./lighthouse": "./src/lighthouse.ts",
    "./shared": "./src/shared.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
