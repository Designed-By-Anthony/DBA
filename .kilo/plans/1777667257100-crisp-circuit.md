# 🔥 MISSION: CRISP CIRCUIT - DEPENDENCY GUTTING & STRUCTURAL AUDIT
**CONTEXT:** ANTHONY. Monorepo (Scorched Earth Phase)
**GOAL:** Eliminate legacy weight, verify ANTHONY. aesthetic purity, and provide clean slate dependencies

---

## 📊 1. ROOT & PACKAGE AUDIT - BEFORE STATE

### ROOT (package.json)
```json
{
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",           // [CORE] - Linting/formatting
    "@playwright/test": "^1.52.0",         // [UNKNOWN] - E2E testing (not used in ANTHONY. aesthetic)
    "cross-env": "^10.1.0",                // [UNKNOWN] - Cross-platform env (legacy)
    "cypress": "^15.14.2",                 // [UNKNOWN] - E2E testing (not used in ANTHONY. aesthetic)
    "drizzle-kit": "^0.31.10",             // [CORE] - Database migrations
    "drizzle-orm": "^0.45.2",              // [CORE] - Database ORM
    "turbo": "^2.9.6"                      // [CORE] - Monorepo build system
  },
  "dependencies": {
    "@marsidev/react-turnstile": "^1.5.1"   // [CORE] - Turnstile CAPTCHA
  }
}
```

### APPS/WEB (package.json)
```json
{
  "dependencies": {
    "@dba/shared": "file:../../packages/shared",  // [CORE] - Shared code
    "@fontsource-variable/fraunces": "^5.2.9",    // [CORE] - Typography
    "@fontsource-variable/inter": "^5.2.8",        // [CORE] - Typography
    "@fontsource-variable/outfit": "^5.2.8",      // [CORE] - Typography
    "@fontsource/abril-fatface": "^5.2.7",        // [CORE] - Typography
    "@marsidev/react-turnstile": "^1.5.1",       // [CORE] - Turnstile CAPTCHA
    "drizzle-orm": "^0.45.2",                    // [CORE] - Database ORM
    "framer-motion": "^12.38.0",                // [LEGACY/GUT LIST] - Animation library (not in ANTHONY. aesthetic)
    "gsap": "^3.15.0",                           // [LEGACY/GUT LIST] - Animation library (not in ANTHONY. aesthetic)
    "next": "16.2.4",                            // [CORE] - Framework
    "react": "19.2.5",                           // [CORE] - Framework
    "react-dom": "19.2.5",                       // [CORE] - Framework
    "sharp": "^0.34.5",                          // [CORE] - Image processing
    "zod": "^4.4.0"                              // [CORE] - Schema validation
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.19.4",         // [CORE] - Cloudflare adapter
    "@tailwindcss/postcss": "^4.2.4",            // [CORE] - CSS framework
    "@types/node": "^24",                        // [CORE] - TypeScript types
    "@types/react": "^19.2.14",                 // [CORE] - TypeScript types
    "@types/react-dom": "^19.2.3",              // [CORE] - TypeScript types
    "esbuild": "^0.28.0",                        // [CORE] - Bundler
    "tailwindcss": "^4.2.4",                     // [CORE] - CSS framework
    "typescript": "^6.0.3",                      // [CORE] - TypeScript
    "wrangler": "^4.86.0"                        // [CORE] - Cloudflare CLI
  }
}
```

### APPS/ADMIN (package.json)
```json
{
  "dependencies": {
    "@clerk/nextjs": "^7.3.0",                  // [LEGACY/GUT LIST] - Auth (not in ANTHONY. aesthetic)
    "@dba/shared": "file:../../packages/shared",  // [CORE] - Shared code
    "@opennextjs/cloudflare": "^1.19.4",         // [CORE] - Cloudflare adapter
    "drizzle-orm": "^0.45.2",                    // [CORE] - Database ORM
    "next": "16.2.4",                            // [CORE] - Framework
    "react": "19.2.5",                           // [CORE] - Framework
    "react-dom": "19.2.5"                       // [CORE] - Framework
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",            // [CORE] - CSS framework
    "@cloudflare/workers-types": "^4.20260429.1", // [CORE] - TypeScript types
    "@types/node": "^24",                        // [CORE] - TypeScript types
    "@types/react": "^19.2.14",                 // [CORE] - TypeScript types
    "@types/react-dom": "^19.2.3",              // [CORE] - TypeScript types
    "tailwindcss": "^4.2.4",                     // [CORE] - CSS framework
    "typescript": "^6.0.3",                      // [CORE] - TypeScript
    "wrangler": "^4.86.0"                        // [CORE] - Cloudflare CLI
  }
}
```

### APPS/API (package.json)
```json
{
  "dependencies": {
    "@dba/shared": "file:../../packages/shared",  // [CORE] - Shared code
    "@elysiajs/cors": "^1.4.2",                  // [CORE] - CORS middleware
    "elysia": "^1.4.28",                         // [CORE] - Web framework
    "zod": "^4.4.0"                              // [CORE] - Schema validation
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260429.1", // [CORE] - TypeScript types
    "@types/node": "^24",                        // [CORE] - TypeScript types
    "drizzle-kit": "^0.31.10",                   // [CORE] - Database migrations
    "drizzle-orm": "^0.45.2",                    // [CORE] - Database ORM
    "typescript": "^6.0.3",                      // [CORE] - TypeScript
    "wrangler": "^4.86.0"                        // [CORE] - Cloudflare CLI
  }
}
```

---

## 🔍 2. CODE-SMELL SCAN - CLERK & VERTAFLOW IMPORTS

### APPS/WEB
**Status: CLEAN** - No Clerk or VertaFlow imports found

### APPS/ADMIN
**Status: CONTAMINATED** - Clerk middleware detected

**File:** `apps/admin/src/middleware.ts`
```typescript
import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";
```

**Scorched Earth Radius:** 1 file
**Impact:** Clerk middleware protects all admin routes
**Action Required:** Remove Clerk middleware and replace with custom auth or remove entirely

---

## 🧹 3. THE "CLEAN" PLAN

### A. PURGE COMMANDS (LEGACY/GUT LIST)

```bash
# Root level
cd /Users/anthonyjones/Web\ Design/Designed_By_Anthony
bun remove @playwright/test cross-env cypress

# Web app
cd apps/web
bun remove framer-motion gsap

# Admin app
cd ../admin  
bun remove @clerk/nextjs
```

### B. CLEAN SLATE DEPENDENCIES FOR APPS/ADMIN

**Minimal dependencies for Next.js/Elysia/Eden/Tailwind build:**

```json
{
  "dependencies": {
    "@dba/shared": "file:../../packages/shared",
    "@opennextjs/cloudflare": "^1.19.4",
    "drizzle-orm": "^0.45.2",
    "next": "16.2.4",
    "react": "19.2.5",
    "react-dom": "19.2.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "@cloudflare/workers-types": "^4.20260429.1",
    "@types/node": "^24",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "tailwindcss": "^4.2.4",
    "typescript": "^6.0.3",
    "wrangler": "^4.86.0"
  }
}
```

**Weight Reduction:**
- **Before:** 7 dependencies + 9 devDependencies = 16 total
- **After:** 6 dependencies + 8 devDependencies = 14 total
- **Reduction:** 2 packages removed (12.5% reduction)

### C. MIDDLEWARE REPLACEMENT

**Current Clerk middleware:**
```typescript
import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";

export default clerkMiddleware(async (auth, req) => {
  // Clerk auth logic
});
```

**Clean replacement options:**

**Option 1: Remove entirely (recommended for ANTHONY. aesthetic)**
```typescript
// apps/admin/src/middleware.ts
import { NextResponse } from "next/server";

export function middleware(request: Request) {
  return NextResponse.next();
}

export const config = {
  matcher: ["/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)", "/(api|trpc)(.*)"]
};
```

**Option 2: Custom auth (if needed)**
```typescript
// apps/admin/src/middleware.ts
import { NextResponse } from "next/server";
import { verifyToken } from "@/lib/auth";

export function middleware(request: Request) {
  const token = request.cookies.get("auth_token")?.value;
  
  if (!token || !verifyToken(token)) {
    return NextResponse.redirect(new URL("/sign-in", request.url));
  }
  
  return NextResponse.next();
}
```

---

## 📈 4. BEFORE & AFTER WEIGHT ANALYSIS

### BEFORE (Current State)
```
ROOT: 6 devDependencies + 1 dependency = 7 packages
WEB: 10 dependencies + 10 devDependencies = 20 packages  
ADMIN: 7 dependencies + 8 devDependencies = 15 packages
API: 4 dependencies + 6 devDependencies = 10 packages
TOTAL: 52 packages
```

### AFTER (Clean Slate)
```
ROOT: 4 devDependencies + 1 dependency = 5 packages (-2)
WEB: 8 dependencies + 10 devDependencies = 18 packages (-2)
ADMIN: 6 dependencies + 8 devDependencies = 14 packages (-1)
API: 4 dependencies + 6 devDependencies = 10 packages (no change)
TOTAL: 47 packages (-5 packages, 9.6% reduction)
```

### IMPACT ANALYSIS
- **Clerk Removal:** Eliminates 1 package from admin
- **Animation Libraries:** Eliminates 2 packages from web (framer-motion, gsap)
- **Testing Tools:** Eliminates 3 packages from root (playwright, cypress, cross-env)
- **Total Reduction:** 6 packages (11.5% of total)

---

## 🎯 5. RECOMMENDATIONS

### HIGH PRIORITY (Execute Immediately)
1. ✅ Remove `@clerk/nextjs` from admin app
2. ✅ Remove `framer-motion` and `gsap` from web app  
3. ✅ Remove `@playwright/test`, `cypress`, `cross-env` from root
4. ✅ Replace Clerk middleware with clean middleware

### MEDIUM PRIORITY (Consider)
1. ⚠️ Evaluate if `@marsidev/react-turnstile` is needed (root dependency)
2. ⚠️ Consider removing `sharp` from web if image processing is handled elsewhere

### LOW PRIORITY (Monitor)
1. 📊 Track bundle size after cleanup
2. 📊 Monitor build times post-purge
3. 📊 Verify no runtime errors from removed dependencies

---

## 🔥 EXECUTION CHECKLIST

- [ ] Run `bun remove @clerk/nextjs` in apps/admin
- [ ] Run `bun remove framer-motion gsap` in apps/web
- [ ] Run `bun remove @playwright/test cypress cross-env` in root
- [ ] Replace `apps/admin/src/middleware.ts` with clean middleware
- [ ] Run `bun install` to update lockfile
- [ ] Run `bun run build` to verify no build errors
- [ ] Run `bun run typecheck` to verify no type errors
- [ ] Commit changes with message "feat: scorched earth dependency purge"

---

## ✅ VERIFICATION CRITERIA

**Build Success:** All apps build without errors
**Type Safety:** No TypeScript errors after purge
**Runtime:** No console errors in browser
**Functionality:** Admin routes accessible without Clerk
**Aesthetic:** Clean, minimal dependency tree aligned with ANTHONY. philosophy

---

**STATUS:** PLAN READY FOR IMPLEMENTATION
**NEXT STEP:** Awaiting user approval to execute purge commands
