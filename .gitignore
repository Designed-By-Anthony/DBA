node_modules
.env
.env.*
!.env.example
.DS_Store
.next
dist
.turbo
.vercel
*.log
playwright-report
test-results
cypress/screenshots/
.superpowers/
# Local tooling / secrets (never commit)
cloud-sql-proxy
dba-website-prod-*.json
apps/web-viewer/zapier-platform-example-app-github/
.env*.local

# Removed app; delete locally if present (some installs had root-owned node_modules):
apps/lighthouse/

# Local tooling downloads
google-cloud-sdk/
google-cloud-cli-darwin-arm.tar.gz

# TypeScript incremental build info
tsconfig.tsbuildinfo

# pnpm lock (project uses npm)
pnpm-lock.yaml
