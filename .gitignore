node_modules
.env
.env.*
!.env.example
.DS_Store
.next
dist
.turbo
.vercel
*.log
playwright-report
test-results
cypress/screenshots/
.superpowers/
# Local tooling / secrets (never commit)
cloud-sql-proxy
dba-website-prod-*.json
apps/web-viewer/zapier-platform-example-app-github/
.env*.local

# Cloudflare Workers build output and local dev secrets
.open-next/
.dev.vars

# Removed app; delete locally if present (some installs had root-owned node_modules):
apps/lighthouse/

# Local tooling downloads
google-cloud-sdk/
google-cloud-cli-darwin-arm.tar.gz

# TypeScript incremental build info
tsconfig.tsbuildinfo

# pnpm lock (project uses bun)
pnpm-lock.yaml
# npm lock (project migrated to bun)
package-lock.json

# worker package
worker/node_modules
worker/.wrangler
