node_modules
.pnpm-store
.env
.env.*
!.env.example
.DS_Store
.next
dist
.turbo
.vercel
*.log
# pnpm monorepo — npm lockfiles in workspace apps cause Vercel to use npm instead of pnpm
apps/*/package-lock.json

# Local tooling / secrets (never commit)
cloud-sql-proxy
dba-website-prod-*.json
apps/web-viewer/zapier-platform-example-app-github/
.env*.local
