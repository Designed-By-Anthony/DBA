"use client";

/**
 * Roofer / trades vertical — job estimator entry (placeholder).
 */
export default function JobEstimator() {
  return (
    <div className="rounded-xl border border-glass-border bg-surface-2 px-4 py-3 text-sm text-text-gray">
      <span className="font-semibold text-white">Job estimator</span>
      <span className="mx-2 text-text-muted">·</span>
      Roofer vertical — scope jobs and estimates from here.
    </div>
  );
}
