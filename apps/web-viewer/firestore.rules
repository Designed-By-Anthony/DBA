rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Security rules are bypassed by the Firebase Admin SDK inside Next.js Server Actions.
      // This file exists solely to satisfy the Firebase Local Emulator startup requirement.
      allow read, write: if true;
    }
  }
}
