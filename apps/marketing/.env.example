# Copy to `.env` for local overrides (not committed).

# Sentry (browser DSN). Used by the Astro SDK and by `npm run sync:firebase-csp` to set CSP
# `report-uri` / Report-To → Sentry Security Policy reporting (CSP violations).
# PUBLIC_SENTRY_DSN=https://...@o....ingest...sentry.io/PROJECT_ID
# Optional: tag CSP reports with environment in Sentry (defaults omitted if unset).
# SENTRY_CSP_ENVIRONMENT=production

# Playwright: Firebase Hosting emulator uses port 5500 (see firebase.json emulators.hosting; avoids :5000 / AirPlay).
# npm run test:e2e:hosting sets this automatically.
# PLAYWRIGHT_USE_FIREBASE_EMULATOR=1

# IndexNow (build-time URL submission). Defaults to the key file in `public/{INDEXNOW_KEY}.txt`.
# INDEXNOW_KEY=a503ff689a18407993ac047df017d9a8
# Optional: override the global endpoint or disable partner fallbacks (comma-separated; empty = no fallbacks).
# INDEXNOW_ENDPOINT=https://api.indexnow.org/indexnow
# INDEXNOW_FALLBACK_ENDPOINTS=https://yandex.com/indexnow,https://search.seznam.cz/indexnow

# Lead forms: POST JSON to Agency OS (CRM). Full URL to POST /api/lead:
# PUBLIC_CRM_LEAD_URL=https://viewer.designedbyanthony.com/api/lead
# Lighthouse (audits / reports only — not the public lead API):
# PUBLIC_API_URL=https://lighthouse-audit--lighthouse-492701.us-east4.hosted.app

# Cloudflare Turnstile widget site key. Defaults: test key in `astro dev`, production key in builds.
# For `astro preview` on localhost, set the test key so the widget matches your API’s test secret:
# PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
# @see https://developers.cloudflare.com/turnstile/troubleshooting/testing/
# PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000BB
